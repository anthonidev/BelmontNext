import React from "react";

type Props = {};

export interface IFormSend {
  fullname: string;
}

const TextAreaMail = (props: Props) => {
  return <h1 className="text-area-mail">xd</h1>;
};

export default TextAreaMail;

import tw from "twin.macro";

const Layout = tw.main` bg-gray-900`;
const Container = tw.div`max-w-7xl h-screen md:mx-auto flex flex-col justify-center items-center mx-4`;
const ContainerIndex = tw.div`max-w-7xl  lg:mx-auto  mx-4 mt-4 `;

export { Layout, Container, ContainerIndex };
